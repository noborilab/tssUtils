% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motif.R
\name{scanPromoterMotifs}
\alias{scanPromoterMotifs}
\title{Scan a strand-oriented window around each promoter for motif occurrences.}
\usage{
scanPromoterMotifs(TSS, genome, pwms, window = c(-50L, 10L),
  threshold = 1e-04, thresholdType = "pvalue", dominantPos = NULL,
  ignoreStrand = FALSE, nthreads = 1L)
}
\arguments{
\item{TSS}{A \code{GRanges} of TSSs (with \code{mcols$name}).}

\item{genome}{A \code{BSgenome} object, an \code{\link[Rsamtools:FaFile-class]{Rsamtools::FaFile()}}, or a character
path to an indexed FASTA (its \code{.fai} index must exist).}

\item{pwms}{A \code{universalmotif} object, a list of them, or a universalmotif
list. Conversion to a log-odds PWM, both-strand scanning, and thresholding
are all handled by \code{\link[universalmotif:scan_sequences]{universalmotif::scan_sequences()}}.}

\item{window}{Integer length-2 vector \code{c(upstream, downstream)} of offsets
relative to the dominant TSS, in TSS-strand orientation (negative =
upstream). The default \code{c(-50L, 10L)} spans the canonical core-promoter
window.}

\item{threshold, thresholdType}{Passed to \code{\link[universalmotif:scan_sequences]{universalmotif::scan_sequences()}}
as \code{threshold} and \code{threshold.type}. The default
\code{threshold = 1e-4}, \code{thresholdType = "pvalue"} is a per-motif significance
cutoff; use \code{thresholdType = "logodds"} with \code{threshold = 0.8} for the
"80\% of maximal log-odds" behaviour.}

\item{dominantPos}{Optional integer vector (one per promoter) giving the
genomic position that anchors each window. Defaults to \code{start(TSS)}.}

\item{ignoreStrand}{If \code{FALSE} (default), both genomic strands are scanned
and each hit is labelled sense or antisense relative to its TSS. If \code{TRUE},
only the oriented sense sequence is scanned.}

\item{nthreads}{Number of threads for \code{\link[universalmotif:scan_sequences]{universalmotif::scan_sequences()}}
(the dominant cost). Defaults to \code{1}; set higher (e.g.
\code{parallel::detectCores() - 2}) to scan a large motif library faster.}
}
\value{
A list of class \code{"promoterMotifScan"} with two elements:
\code{occurrences}, a \code{GRanges} with one row per hit (genomic coordinates and
strand, plus mcols \code{promoter}, \code{motif}, \code{score}, \code{offset}, \code{relStrand},
\code{tssStrand}); and \code{matrices}, a list of \code{presence}, \code{count}, and
\code{bestScore} matrices of dimension \verb{length(TSS) × nMotif}. The \code{offset} mcol
is the signed distance, in TSS orientation (negative = upstream), of the
hit's leftmost base in the oriented window from the dominant TSS; for an
antisense hit that leftmost base is the motif's 3' end on its own strand.
}
\description{
For each promoter, a window is defined relative to the dominant TSS position
(in TSS-strand orientation), its genomic sequence is fetched, and a motif
library is scanned against it with \code{\link[universalmotif:scan_sequences]{universalmotif::scan_sequences()}}. The
function returns both the per-occurrence hits (for the position-resolved
meta-profile in \code{\link[=motifAnchoredDelta]{motifAnchoredDelta()}}) and the per-promoter
presence/count/best-score matrices (for the regression in
\code{\link[=shapeMotifActivity]{shapeMotifActivity()}}), so a single scan feeds both analyses.
}
\details{
The anchor position is best taken from \code{\link[=aggregateTSSShape]{aggregateTSSShape()}}, e.g.
\code{dominantPos = aggregateTSSShape(shape, TSS)$thickStart}, which keeps motif
scanning independent of any particular signal list.
}
\seealso{
\code{\link[=shapeMotifActivity]{shapeMotifActivity()}}, \code{\link[=motifAnchoredDelta]{motifAnchoredDelta()}},
\code{\link[=aggregateTSSShape]{aggregateTSSShape()}}.
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@tsl.ac.uk}
}
