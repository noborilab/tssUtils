% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{annotateTSS}
\alias{annotateTSS}
\title{Annotate TSSs against transcript and feature reference data.}
\usage{
annotateTSS(TSS, txData, featureSets = list(), tx5utr = NULL,
  peakAnnotations = list(), geneSymbols = NULL,
  geneFromTxFn = function(x) sub("\\\\.\\\\d+$", "", x),
  teGenes = character(), featureCoding = character(), mRNATypes = "mRNA",
  promoterUpstream = 200, promoterDownstream = 200, divergentDist = 500)
}
\arguments{
\item{TSS}{A \code{GRanges} of called TSSs. Must have \code{mcols(TSS)$name} set; it
will be set to \code{paste0("TSS_", seq_along(TSS))} if missing.}

\item{txData}{Output of \code{\link[=prepTxData]{prepTxData()}}.}

\item{featureSets}{Named list of \code{\link[=prepFeatureData]{prepFeatureData()}} outputs (e.g.
\code{list(TE = teData)}). The list order is the order classification rules
are applied. May be empty.}

\item{tx5utr}{Optional \code{GRanges} of 5'UTR ranges (must have \code{mcols$name}
matching transcript IDs in \code{txData$feat}).}

\item{peakAnnotations}{Optional named list of \code{GRanges} (e.g. enhancer or
ATAC peak annotations). For each entry \code{nm}, an \verb{Overlapping<nm>} column
is added to the result.}

\item{geneSymbols}{Optional \code{data.frame} with columns \code{Gene} and \code{Symbol}
giving a transcript-ID-stripped → symbol lookup.}

\item{geneFromTxFn}{Function applied to \code{FeatureID} to derive a gene ID
that can be looked up in \code{geneSymbols$Gene}. Defaults to stripping a
trailing \verb{.<digits>}.}

\item{teGenes}{Optional character vector of gene IDs (post-\code{geneFromTxFn})
that should be reclassified as \code{transposable_element_gene} /
\code{teTSS} even when they were classified as \code{pcTSS} based on \code{mRNATypes}.}

\item{featureCoding}{Named character vector mapping each feature-set name
to the \code{TSSTypeCoding} value to assign on a sense promoter / antisense
match (e.g. \code{c(TE = "teTSS")}). Defaults to \code{"teTSS"} for any unnamed
entries.}

\item{mRNATypes}{Character vector of \code{featAnn[, txData$typeCol]} values
that mark a sense Tx promoter as \code{pcTSS}. Other values stay \code{ncTSS}.}

\item{promoterUpstream, promoterDownstream}{Distance thresholds (bp) for
the promoter rules. Sense Tx promoter accepts overlap with the
transcript 5'UTR, distance 0, or upstream within \code{promoterUpstream}.
Other promoter rules use \code{promoterUpstream} only. \code{promoterDownstream}
is reserved for future extensions.}

\item{divergentDist}{Maximum distance between paired antisense pcTSS/ncTSS
to flag as divergent.}
}
\value{
A \code{data.frame} with one row per \code{TSS} and columns: \code{TSS}, \code{Chr},
\code{Start}, \code{End}, \code{Strand}, \code{TSSTypeCoding}, \code{TSSTypeLocation},
\code{FeatureID}, \code{FeatureSymbol}, \code{FeatureType}, \code{TSSFeatOrientation},
\code{DistanceToFeature}, \code{IsDivergent}, \code{DivergentDistance}, \code{DivergentTSS},
plus one \verb{Overlapping<name>} column per \code{peakAnnotations} entry.
}
\description{
Each TSS is classified by looking at how it overlaps, and how close it sits
to, the transcript and feature sets you supply. The rules are applied in a
fixed order of precedence, from highest to lowest: sense Tx promoter (which
is 5'UTR-aware), sense feature promoter, any other sense Tx promoter,
intragenic sense Tx, intragenic sense feature, antisense Tx, antisense
feature, and finally intergenic (the catch-all for anything that is left
over). Once everything has been classified, divergent protein-coding /
non-coding TSS pairs are flagged with \code{\link[=findDivergent]{findDivergent()}}.
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@tsl.ac.uk}
}
