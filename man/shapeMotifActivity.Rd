% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapeActivity.R
\name{shapeMotifActivity}
\alias{shapeMotifActivity}
\title{MARA-style regularized regression of delta-shape on promoter motif content.}
\usage{
shapeMotifActivity(deltaShape, motifMatrix, covariates = NULL, alpha = 0.5,
  penalizeCovariates = FALSE, standardize = TRUE, nfolds = 10,
  lambda = c("lambda.1se", "lambda.min"), significance = c("permutation",
  "stability", "none"), nperm = 1000, nstab = 100, stabSubsample = 0.5,
  stabThreshold = 0.6)
}
\arguments{
\item{deltaShape}{Named numeric response vector, e.g. from
\code{\link[=deltaShapeVector]{deltaShapeVector()}}.}

\item{motifMatrix}{A promoter-by-motif matrix (rownames = promoter), such as
\code{scanPromoterMotifs(...)$matrices$presence}. Position-binned columns are
treated as independent predictors.}

\item{covariates}{Optional \code{data.frame} of per-promoter covariates (aligned
by rowname), turned into a model matrix and added as predictors.}

\item{alpha}{glmnet elastic-net mixing parameter (default 0.5).}

\item{penalizeCovariates}{If \code{FALSE} (default), covariate columns are left
unpenalized (always retained); if \code{TRUE}, they are penalized like motifs.}

\item{standardize}{Passed to glmnet (default \code{TRUE}).}

\item{nfolds}{Number of cross-validation folds for lambda selection.}

\item{lambda}{Which cross-validated lambda to use, \code{"lambda.1se"} (default,
conservative) or \code{"lambda.min"}.}

\item{significance}{\code{"permutation"} (default), \code{"stability"}, or \code{"none"}.}

\item{nperm}{Number of permutations for \code{significance = "permutation"}.}

\item{nstab}{Number of subsamples for \code{significance = "stability"}.}

\item{stabSubsample}{Fraction of promoters per stability subsample.}

\item{stabThreshold}{Selection-frequency threshold reported for stability.}
}
\value{
A \code{data.frame}, one row per motif column, sorted by descending
absolute coefficient, with columns \code{motif}, \code{coef}, \code{sign}, \code{pPerm},
\code{selectionFreq}, and \code{lambda}. Attributes \code{nUsed}, \code{nDropped}, and \code{alpha}
record the fit. Covariate coefficients are not returned.
}
\description{
Fits an elastic-net (glmnet) model of a per-promoter delta-shape response on
the per-promoter motif matrix, optionally adjusting for covariates. The
signed coefficient per motif is its estimated shape-activity change between
conditions; the regularization deconvolves collinear motifs. Significance is
assessed by permutation or stability selection, since glmnet does not give
usable p-values directly.
}
\details{
The cross-validation, permutations, and stability subsamples are
random; call \code{\link[=set.seed]{set.seed()}} beforehand for reproducible results.
}
\seealso{
\code{\link[=deltaShapeVector]{deltaShapeVector()}}, \code{\link[=scanPromoterMotifs]{scanPromoterMotifs()}}.
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@tsl.ac.uk}
}
