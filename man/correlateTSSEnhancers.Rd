% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation.R
\name{correlateTSSEnhancers}
\alias{correlateTSSEnhancers}
\title{Correlate TSS expression with enhancer expression.}
\usage{
correlateTSSEnhancers(quantTSS, quantEnh, TSS, enh, minPCC = 0.5,
  anno = NULL, codingOnly = FALSE)
}
\arguments{
\item{quantTSS}{A numeric matrix or \code{data.frame} of TSS quantifications
(rows aligned with \code{TSS}).}

\item{quantEnh}{A numeric matrix or \code{data.frame} of enhancer
quantifications (rows aligned with \code{enh}).}

\item{TSS}{A \code{GRanges} of TSSs.}

\item{enh}{A \code{GRanges} of enhancers.}

\item{minPCC}{Minimum Pearson correlation to retain.}

\item{anno}{Optional annotation \code{data.frame} from \code{\link[=annotateTSS]{annotateTSS()}}; required
when \code{codingOnly = TRUE}.}

\item{codingOnly}{If \code{TRUE}, restrict to TSSs with
\code{anno$TSSTypeCoding == "pcTSS"}.}
}
\value{
A \code{data.frame} with columns \code{TSS}, \code{Enhancer}, \code{PCC}, \code{Dist}.
}
\description{
For each TSS-enhancer pair on the same chromosome, computes a Pearson
correlation across samples on log2(1 + CPM)-transformed values. Pairs with
\code{PCC < minPCC} are dropped, as are self-pairs (\code{Dist == 0}). Distances are
computed via \code{\link[GenomicRanges:nearest-methods]{GenomicRanges::distance()}}.
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@tsl.ac.uk}
}
