% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapeActivity.R
\name{motifAnchoredDelta}
\alias{motifAnchoredDelta}
\title{Position-resolved meta-profile of delta 5' signal around a motif.}
\usage{
motifAnchoredDelta(countSignalListA, countSignalListB, motifGR,
  window = c(100, 100), agg = c("mean", "median"), ci = c("bootstrap",
  "normal", "none"), ciLevel = 0.95, nboot = 1000,
  combineSamples = c("sum", "mean"), control = c("shuffle", "background",
  "none"), backgroundGR = NULL, nShuffle = 1, shuffleBy = 200,
  scoreCol = "score")
}
\arguments{
\item{countSignalListA, countSignalListB}{Lists of stranded count \code{GRanges}
(one per sample) for conditions A (baseline) and B (contrast).}

\item{motifGR}{A single-motif occurrence \code{GRanges}; strand is required (no
\code{*}).}

\item{window}{Integer length-2 vector \code{c(upstream, downstream)} of the
profile half-widths, in motif-5' orientation.}

\item{agg}{Across-occurrence aggregation, \code{"mean"} (default) or \code{"median"}.}

\item{ci}{Confidence band: \code{"bootstrap"} (default), \code{"normal"}, or \code{"none"}.}

\item{ciLevel}{Confidence level (default 0.95).}

\item{nboot}{Bootstrap resamples for \code{ci = "bootstrap"}.}

\item{combineSamples}{How to combine samples within a condition, \code{"sum"}
(default, raw counts) or \code{"mean"}. With \code{"sum"} the two conditions should
have the same number of samples, otherwise the delta is confounded by
library count; use \code{"mean"} for unequal replicate counts (a warning is
issued in that case).}

\item{control}{Null band: \code{"shuffle"} (default; shift windows by \code{shuffleBy}
bp, strand-aware), \code{"background"} (use \code{backgroundGR}), or \code{"none"}.}

\item{backgroundGR}{For \code{control = "background"}, a stranded \code{GRanges} of
motif-free anchor sites; subsampled to the occurrence count when larger.}

\item{nShuffle}{Number of shuffles to average for \code{control = "shuffle"}.}

\item{shuffleBy}{Shift size in bp for \code{control = "shuffle"}.}

\item{scoreCol}{Score column name in the signal \code{GRanges}.}
}
\value{
A \code{data.frame} with \code{window[1] + window[2] + 1} rows and columns
\code{distance}, \code{deltaMean}, \code{deltaLo}, \code{deltaHi}, \code{signalA}, \code{signalB},
\code{deltaAntisense}, \code{control}, and \code{n}. Attributes \code{window}, \code{nOccurrences},
and \code{agg} record the call.
}
\description{
For the occurrences of a single motif, extracts per-position 5' signal in two
conditions over a fixed, strand-oriented window centred on each motif, and
returns condition B minus condition A aggregated across occurrences, as a
meta-profile of delta-signal versus distance from the motif. A localized
peak or dip at a stereotyped offset is the signature of a direct local
effect; compare it against the control band.
}
\details{
Pass a \code{motifGR} already restricted to a single motif (subset
\code{scanPromoterMotifs(...)$occurrences}); facet over motifs by looping.

The bootstrap confidence band and the shuffle/background control
draw random samples; call \code{\link[=set.seed]{set.seed()}} beforehand for reproducible results.
}
\seealso{
\code{\link[=scanPromoterMotifs]{scanPromoterMotifs()}}.
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@tsl.ac.uk}
}
