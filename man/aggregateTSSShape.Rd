% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape.R
\name{aggregateTSSShape}
\alias{aggregateTSSShape}
\title{Aggregate per-sample TSS shape into cross-sample peak coordinates.}
\usage{
aggregateTSSShape(shape, TSS, method = c("max", "median"))
}
\arguments{
\item{shape}{List of matrices returned by \code{\link[=tssShape]{tssShape()}} (must contain
\code{maxPos} and \code{maxScore}).}

\item{TSS}{A \code{GRanges} of TSSs (rows aligned with \code{shape$maxPos}).}

\item{method}{Either \code{"max"} (per-TSS sample with highest score) or
\code{"median"} (across-sample median peak position).}
}
\value{
A \code{GRanges} mirroring \code{TSS} with integer \code{thickStart}/\code{thickEnd}
mcols.
}
\description{
Produces a \code{GRanges} whose ranges match the input \code{TSS}, where the
\code{thickStart} and \code{thickEnd} mcols give either the peak position from the
single sample with the highest score (\code{method = "max"}), or the median peak
position taken across all the samples (\code{method = "median"}).
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@tsl.ac.uk}
}
