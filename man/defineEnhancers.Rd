% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enhancer.R
\name{defineEnhancers}
\alias{defineEnhancers}
\title{Define enhancer regions from annotated TSSs.}
\usage{
defineEnhancers(TSS, anno, exprFilt, divergentDist = 500,
  intergenicFlank = 500, intergenicTypes = c("transposable_element",
  "transposable_element_gene", "antisense_lncRNA", "antisense_RNA", "lnc_RNA",
  "miRNA_primary_transcript", "ncRNA", "pseudogenic_transcript",
  "transcript_region"))
}
\arguments{
\item{TSS}{A \code{GRanges} of called TSSs (with \code{mcols$name} matching \code{anno$TSS}).}

\item{anno}{Annotation \code{data.frame} produced by \code{\link[=annotateTSS]{annotateTSS()}}.}

\item{exprFilt}{Logical vector aligned with \code{anno}/\code{TSS} indicating which
TSSs pass the expression filter (e.g. from \code{\link[=filterByExpression]{filterByExpression()}}).}

\item{divergentDist}{Maximum distance between paired antisense ncTSSs that
counts as bidirectional.}

\item{intergenicFlank}{Upstream extension applied to unidirectional
intergenic ncTSSs.}

\item{intergenicTypes}{Feature types treated as intergenic-equivalent for
the unidirectional rule.}
}
\value{
A list with three \code{GRanges} slots: \code{bdGR} (bidirectional regions),
\code{intTSS} (unidirectional intergenic regions), and \code{enh} (the sorted union
of both, with \code{name} mcol).
}
\description{
Builds two kinds of enhancer-like region and hands them back together with
their merged union:
}
\details{
\itemize{
\item \strong{Bidirectional ncTSS regions}: pairs of antisense ncTSSs whose ends fall
within \code{divergentDist} bp of each other, with the span between the pair
taken as the region.
\item \strong{Unidirectional intergenic ncTSS regions}: ncTSSs that were classified as
\code{Intergenic} (or as one of \code{intergenicTypes}) and are not divergent, each
extended \code{intergenicFlank} bp upstream of its 5' end.
}

Any region that ends up overlapping a pcTSS is dropped, since it is more
likely to belong to a known promoter than to an enhancer.
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@tsl.ac.uk}
}
