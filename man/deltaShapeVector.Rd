% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shapeActivity.R
\name{deltaShapeVector}
\alias{deltaShapeVector}
\title{Per-promoter delta-shape response vector between two conditions.}
\usage{
deltaShapeVector(shapeA, shapeB, metric = c("entropy", "width", "modeShift",
  "directionality"), pctLo = NULL, pctHi = NULL, aggregate = c("mean",
  "median"))
}
\arguments{
\item{shapeA, shapeB}{\code{\link[=tssShape]{tssShape()}} / \code{\link[=tssShapeMatched]{tssShapeMatched()}} outputs for the two
conditions (A is the baseline, B the contrast).}

\item{metric}{Which shape statistic to difference:
\describe{
\item{\code{"entropy"}}{Shannon entropy (\code{shannon}).}
\item{\code{"width"}}{Interquantile width, \code{pctHi - pctLo} in bp; requires
\code{pctLo} and \code{pctHi}.}
\item{\code{"modeShift"}}{Dominant-peak position (\code{maxPos}), genome-oriented,
so on a minus-strand promoter a positive delta is an upstream shift.}
\item{\code{"directionality"}}{\code{(sumScore - sumScoreAnti) / (sumScore + sumScoreAnti)}; requires shapes built with
\code{antisense = TRUE}.}
}}

\item{pctLo, pctHi}{Integer percentile indices (the \code{N} in \code{pctN}) bounding
the width window. Required for \code{metric = "width"}.}

\item{aggregate}{How to aggregate across samples within a condition, either
\code{"mean"} (default) or \code{"median"}.}
}
\value{
A named numeric vector (names are promoters present in both
conditions) of \code{aggB - aggA}. \code{NA} in either condition yields \code{NA}.
}
\description{
Reduces two (depth-matched) \code{\link[=tssShape]{tssShape()}} outputs to a single number per
promoter, suitable as the response in \code{\link[=shapeMotifActivity]{shapeMotifActivity()}}. Promoters are
aligned by name, the chosen statistic is aggregated across samples within
each condition, and condition B minus condition A is returned.
}
\seealso{
\code{\link[=tssShapeMatched]{tssShapeMatched()}}, \code{\link[=shapeMotifActivity]{shapeMotifActivity()}}.
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@tsl.ac.uk}
}
