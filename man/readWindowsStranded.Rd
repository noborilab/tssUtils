% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{readWindowsStranded}
\alias{readWindowsStranded}
\title{Read stranded signal in windows from a \code{GRanges} object or paired bedGraph/bigWig files}
\usage{
readWindowsStranded(signal, windows, scoreCol = "score", trimSignal = c(0,
  1), keepTopPctile = 0, rowNorm = FALSE, windowAverage = TRUE,
  mergeSignalMatrix = TRUE, signalFactor = c(1, 1),
  negStrandValues = FALSE, ...)
}
\arguments{
\item{signal}{Either a stranded \code{GRanges} object, or a length-2 character
vector of filenames (positive strand first, then negative strand).}

\item{windows}{A stranded \code{GRanges} object containing equal width ranges.}

\item{scoreCol}{The name of the column containing scores if the input is a \code{GRanges} object.}

\item{trimSignal}{Lower and upper fractions of the score to trim.}

\item{keepTopPctile}{Only keep windows which are within the top percentile value.}

\item{rowNorm}{Normalize window scores between 0 and 1.}

\item{windowAverage}{Average signal values across all windows.}

\item{mergeSignalMatrix}{If \code{windowAverage = FALSE}, whether to return a
single sense-minus-antisense matrix or a list with \code{Positive} and
\code{Negative} matrices.}

\item{signalFactor}{Length-2 numeric vector of factors to multiply the
positive- and negative-strand scores by.}

\item{negStrandValues}{If \code{TRUE}, antisense scores are returned as negative
numbers.}

\item{...}{Additional arguments for \code{\link[genomation:ScoreMatrix-methods]{genomation::ScoreMatrix()}}.}
}
\value{
If \code{windowAverage = TRUE}, a \code{data.frame} with columns \code{Position},
\code{SignalPositive}, \code{SignalNegative}, \code{SignalMerged}. Otherwise, a \code{matrix}
(when \code{mergeSignalMatrix = TRUE}) or a list of two matrices.
}
\description{
Computes per-window sense and antisense signal matrices given strand-aware
input signal, and optionally averages across windows.
}
\author{
Benjamin Jean-Marie Tremblay, \email{benjamin.tremblay@tsl.ac.uk}
}
